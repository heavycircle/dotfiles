{
  "layer": "top",
  "position": "top",
  "height": 40,
  "spacing": 20,

  "modules-left": ["hyprland/workspaces"],
  "modules-center": ["hyprland/window"],
  "modules-right": [
    "network",
    "pulseaudio",
    "cpu",
    "memory",
    "battery",
    "clock",
  ],

  "hyprland/workspaces": {
    "disable-scroll": false,
    "all-outputs": true,
    "format": "{id}",
    "on-click": "activate",
    "persistent-workspaces": {
      "*": [1, 2, 3, 4, 5],
    },
  },

  "network": {
    "interval": 2,
    "format-wifi": "󰤨  {essid}",
    "format-ethernet": "  wired",
    "format-linked": "󱘖  {ifname} (no IP)",
    "format-disconnected": "  offline",
    "tooltip": true,
    "tooltip-format": "<b>{ifname}</b>\nSSID: {essid}\nIPv4: {ipaddr}\nUp: {bandwidthUpBytes} /s\nDown: {bandwidthDownBytes} /s",
    "on-click": "uwsm app -- alacritty -e nmtui",
    "on-click-right": "bash -lc 'ip -o -4 addr show up | awk '{print $4}' | cut -d/ -f1 | head -1 | wl-copy'",
  },

  "pulseaudio": {
    "interval": 1,
    "format": "  {volume}%",
    "format-muted": "󰖁 0%",
    "on-click": "pactl set-sink-mute @DEFAULT_SINK@ toggle",
    "on-click-right": "pavucontrol -t 3",
    "tooltip": true,
    "tooltip-format": "Volume: {volume}%\nSink: {desc}",
  },

  "cpu": {
    "interval": 1,
    "format": "  {usage}%",
    "tooltip": true,
  },

  "memory": {
    "interval": 3,
    "format": "  {used:0.1f}G/{total:0.1f}G",
    "tooltip": true,
    "tooltip-format": "Memory: {used:0.2f}G / {total:0.2f}G",
  },

  "battery": {
    "interval": 1,
    "states": { "warning": 30, "critical": 15 },
    "format": "{icon} {capacity}%",
    "format-charging": "󱐋 {capacity}%",
    "format-icons": ["󰂎", "󰁼", "󰁿", "󰂁", "󰁹"],
    "tooltip": true,
    "tooltip-format": "Battery: {capacity}% ({timeTo})",
  },

  "clock": {
    "interval": 1,
    "timezone": "America/New_York",
    "format": "  {:%H:%M}",
    "tooltip": true,
    "tooltip-format": "{:L%Y %m %d, %A}",
  },

  "reload_style_on_change": true,
}
